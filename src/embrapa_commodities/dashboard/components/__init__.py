"""Dash UI components — the app chrome and shared atoms.

Each function returns a Dash component tree using the **design system's own class
names** (``assets/dashboard.css`` is reused verbatim), so the layout inherits the
brand styling without re-authoring CSS.
"""

from __future__ import annotations
